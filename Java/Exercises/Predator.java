public interface Predator {
    // interfaces are a blueprint for a class that specifies a set of abstract methods
    // that implementing classes must define.
    // Supports multi inheritence-like behavior
    void hunt();
}
